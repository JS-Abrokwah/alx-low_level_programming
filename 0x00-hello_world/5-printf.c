#include <stdio.h>

/**
 * main - The program starts execution from here
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", str); /*print format to stdout*/
	return (0); /*exit with success code 0*/
}
