#include "main.h"
#include <stdlib.h>
#include <ctype.h>

/**
 * _atoi - converts string to integer
 * @s: a string parameter
 *
 * Return: the converted string
 */
int _atoi(char *s)
{
	return (atoi(s));
}
