#include "main.h"
#include <string.h>

/**
 * _strlen - compute and return the length of string
 * @s: a pointer parameter
 *
 * Return: integer, the lenght of a string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
