#include "main.h"
#include <stdlib.h>

/**
 * _abs - Computes the absolute value of an integer
 * @n: A parameter that hold the input number
 *
 * Return: The absolute value of n
 */
int _abs(int n)
{
	return (abs(n));
}
